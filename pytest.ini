[pytest]
# The suites import `backend.app.main`, so the repo root must be importable.
# Without this, `pytest backend/tests/` only works by accident depending on how
# pytest was invoked (`python -m pytest` adds CWD to sys.path, plain `pytest`
# does not).
pythonpath = .
testpaths = backend/tests
